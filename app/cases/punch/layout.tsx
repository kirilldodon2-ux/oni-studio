import type { ReactNode } from "react";

export const metadata = {
  title: "PUNCH — Party Event Design · ONI Studio",
  description:
    "Full brand identity case for PUNCH NEVER SLEEP event — event branding, visual identity, merch, and posters.",
};

export default function PunchCaseLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
