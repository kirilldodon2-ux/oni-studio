# `_archive/`

Reference-only exports and source bundles preserved after porting into the active Next.js codebase.

**Not runtime.** Nothing in this directory is imported, built, or deployed.

| Path | Contents |
|------|----------|
| `brandbook-make-export/` | Figma Make Vite export — original Interactive ONI Brandbook Experience |

Active production route: `app/brandbook/` — see `docs/BRANDBOOK_INTEGRATION.md`.

For deprecated site scaffolding removed from active use, see `_archive_graveyard/`.
