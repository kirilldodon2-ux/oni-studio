# Optional archive media CDN origin (no trailing slash).
# Unset locally to use public/archive/objects from the repo.
# Production: set on Cloudflare Pages before build.
# NEXT_PUBLIC_ARCHIVE_MEDIA_ORIGIN=https://pub-xxxx.r2.dev
