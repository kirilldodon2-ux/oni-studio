# Deprecated — do not use

Browse representation authority moved to object folders:

```
public/archive/objects/[slug]/00-hero.[ext]
```

Registry `previewSrc` is derived via `content/archiveObjectPaths.ts` → `canonicalPreviewSrc()`.

Do not add new files here. Remove legacy copies when no longer needed for reference.
